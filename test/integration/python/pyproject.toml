[project]
name = "duckdb-gdx-integration"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "duckdb>=0.10.3",
    "pytest>=8.2",
]

dynamic = []

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

